PORT=3001

# Firebase Admin SDK Configuration
# Obtener desde la consola de Firebase > Configuracion del Proyecto > Cuentas de Servicio
FIREBASE_DATABASE_URL=https://tu-proyecto.firebaseio.com

# Si usas firebase-admin, dejalo como esta
# Si usas firebase SDK (client), usa las variables VITE_ en .env.local
